<Window x:Class="SprawdzianViewModel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SprawdzianViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:ShopVM />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="8*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="Panel Zarządzania Sklepem" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical">
            <Label Content="KLIENCI" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="LightCoral"/>
            <ListBox ItemsSource="{Binding Customers}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="LightGray">
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding FirstName}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding LastName}" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="{Binding OrderCount, StringFormat='Zamównienia: {0}'}" />
                                <TextBlock Text="{Binding TotalSpent, StringFormat='Wydano: {0:F2}'}" />
                                <TextBlock Text="{Binding IsPremium, StringFormat='Premium: {0}'}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontSize" Value="15" />
                </Style>
            </StackPanel.Resources>
            <TextBlock Text="{Binding CustomerCount, StringFormat='Liczba klientów: {0}'}" Margin="0 0 50 0"/>
            <TextBlock Text="{Binding WareHouseValue, StringFormat='Wartość magazynu: {0:F2} zł'}" />
        </StackPanel>
    </Grid>
</Window>
