<Window x:Class="Simple_WPF_Control.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Simple_WPF_Control"
        mc:Ignorable="d"
        Title="MainWindow" Height="240" Width="350">
    <StackPanel HorizontalAlignment="Stretch" Margin="20 0">
        <StackPanel.Resources>
            <Style TargetType="DockPanel">
                <Setter Property="Margin" Value="0 0 0 4" />
            </Style>
        </StackPanel.Resources>
        <Label Content="Simple WPF Control" HorizontalAlignment="Center"/>
        <DockPanel LastChildFill="False" HorizontalAlignment="Stretch">
            <Label DockPanel.Dock="Left" Content="Name" />
            <TextBox x:Name="nameInput" DockPanel.Dock="Right" LostFocus="TextBox_LostFocus" Width="200" VerticalAlignment="Center"/>
        </DockPanel>
        <DockPanel LastChildFill="False" HorizontalAlignment="Stretch">
            <Label DockPanel.Dock="Left" Content="Street Adress" />
            <TextBox DockPanel.Dock="Right" Width="200" VerticalAlignment="Center"/>
        </DockPanel>
        <DockPanel LastChildFill="False" HorizontalAlignment="Stretch">
            <Label DockPanel.Dock="Left" Content="City" />
            <TextBox Width="100" Margin="64 0 0 0" VerticalAlignment="Center"/>
            <TextBox DockPanel.Dock="Right" Width="50"  VerticalAlignment="Center"/>
            <Label DockPanel.Dock="Right" Content="State" />
        </DockPanel>
        <DockPanel LastChildFill="False" HorizontalAlignment="Stretch">
            <Label DockPanel.Dock="Left" Content="Zip" />
            <TextBox Name="zipCodeInput" LostFocus="zipCodeInput_LostFocus" Width="100" Margin="67 0 0 0" VerticalAlignment="Center" />
        </DockPanel>
        <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
            <Button x:Name="OkButton" Content="OK" Width="75" Click="OkButton_Click" />
            <Button x:Name="CancelButton" Content="Cancel" Width="75" Margin="10 0 0 0" Click="CancelButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
