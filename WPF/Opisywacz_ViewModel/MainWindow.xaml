<Window x:Class="Opisywacz_ViewModel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Opisywacz_ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:OpisywaczViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label Content="Autor: Maksim" FontSize="24" Margin=" 0 0 0 20"/>
            <Button Content="Dodaj" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label Content="Cecha 1 - " />
                <TextBox Width="256"/>
                <Button Content="Wyczyść" Margin="20 0 10 0"/>
                <Button Content="Dodaj" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 15 0 15">
                <Label Content="Cecha 2 - " />
                <TextBox Width="256"/>
                <Button Content="Wyczyść" Margin="20 0 10 0"/>
                <Button Content="Dodaj" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label Content="Cecha 3 - " />
                <TextBox Width="256"/>
                <Button Content="Wyczyść" Margin="20 0 10 0"/>
                <Button Content="Dodaj" />
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <ListView>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
