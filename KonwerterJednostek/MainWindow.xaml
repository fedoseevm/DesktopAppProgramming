<Window x:Class="KonwerterJednostek.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KonwerterJednostek"
        mc:Ignorable="d"
        Title="Konwerter jednostek. Wykonał: 04" 
        Height="450" Width="375"
        Background="#D3D3D3">
    <Grid Margin="10">
        <TabControl>

            <TabItem Header="Długość">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBox x:Name="inputText"
                             Text="0"
                             HorizontalContentAlignment="Right"
                             Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <ComboBox x:Name="comboBoxFrom" Width="150" Margin="0,0,10,0"/>
                        <ComboBox x:Name="comboBoxTo" Width="150"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button x:Name="btnConvert"
                                Content="Konwertuj"
                                Width="120"
                                Margin="0,0,10,0"
                                Background="#6495ED"
                                Click="btnConvert_Click"/>

                        <Button x:Name="btnClear"
                                Content="Wyczyść"
                                Width="120"
                                Background="#FA8072"
                                Click="btnClear_Click"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3">
                        <Label x:Name="labelResult"
                               Content="0"
                               Background="White"
                               Padding="5"
                               Margin="0,0,0,10"/>

                        <ListBox x:Name="historyList"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Masa"/>
            <TabItem Header="Temperatura"/>

        </TabControl>
    </Grid>
</Window>
